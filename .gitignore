# macOS
.DS_Store
._*
*.swp

# Editor + tool caches (personal — never commit)
.claude/
.cursor/
.vscode/
.idea/

# Local hosting tool state
.netlify/
.wrangler/
.vercel/

# Node / package managers
node_modules/
.npm/
.pnpm-store/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs (regenerated from source)
admin-dist/
marketing-dist/
dist/
build/

# Env files (NEVER commit — secrets live in Supabase secrets, Netlify env, Cloudflare env, etc.)
.env
.env.*
!.env.example
*.pem
*.key
*.cert

# OS metadata
Thumbs.db
ehthumbs.db
Desktop.ini

# Empty / scratch
3D Project
